function sn(m, t = 'info') {
  const _NT = document.createElement('div');
  _NT.className = `notification ${t}`;
  _NT.innerHTML = `
        <div class="notification-content">
            <i class="fas ${t === 'success' ? 'fa-check-circle' : 'fa-info-circle'}"></i>
            <span>${m}</span>
            <button class="notification-close">
                <i class="fas fa-times"></i>
            </button>
        </div>
    `;
  _NT.style.cssText = `
        position: fixed;
        top: 20px;
        right: 20px;
        background: ${t === 'success' ? '#27ae60' : '#667eea'};
        color: white;
        padding: 15px 20px;
        border-radius: 10px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        z-index: 10001;
        animation: slideInRight 0.3s ease;
        max-width: 400px;
    `;

  document.body.appendChild(_NT);

  // Close _NT
  const closeBtn = _NT.querySelector('.notification-close');
  closeBtn.addEventListener('click', () => {
    _NT.style.animation = 'slideOutRight 0.3s ease';
    setTimeout(() => _NT.remove(), 300);
  });

  // Auto-remove after 5 seconds
  setTimeout(() => {
    if (_NT.parentNode) {
      _NT.style.animation = 'slideOutRight 0.3s ease';
      setTimeout(() => _NT.remove(), 300);
    }
  }, 5000);
}
function _sl() {
  document.querySelectorAll('select').forEach(e => {
    let f = e.parentElement, h = document.createElement('div');
    if (f.querySelector('[selector]')) f.querySelector('[selector]').remove();
    h.setAttribute('selector', 'true'), d = e.querySelectorAll('option'), k = '<div options>', i = 0;
    h.innerHTML += `<selected>${e.querySelector('option').innerHTML}</selected>`;
    for (; i < d.length; i++) {
      if (!d[i].disabled) k += `${d[i].outerHTML}`;
    }
    k += '</div>';
    h.innerHTML += k;
    f.prepend(h);
    i = document.createElement('i');
    i?.classList.add('fas', 'fa-chevron-down', 'o-ble');
    if (f.querySelector('.o-ble')) f.querySelector('.o-ble').remove();
    f.prepend(i);
    e.style.display = 'none';
    _Ac('.o-ble.fa-chevron-down');
    f.querySelectorAll('[selector] [options] option').forEach(x => { x.addEventListener('click', () => { f.querySelector('[selector] selected').textContent = x.getAttribute('value'); e.value = x.getAttribute('value'); x.parentElement?.classList.remove('active'); }); });
    document.addEventListener('click', (o) => { if (!o.target.closest('[options]')) f.querySelector('[options]')?.classList.remove('active'); if (o.target.closest('.o-ble:has(+ [selector])')) f.querySelector('[options]')?.classList.toggle('active') });
  });
}
function cm() {
    m2.style.animation = 'fadeOut 0.3s ease';
    setTimeout(() => {
      m2.remove();
      ss.remove();
    }, 300);
  }
function gb() {
  const m2 = document.createElement('div');
  m2.className = 'feedback-modal';
  m2.innerHTML = `
        <div class="feedback-modal">
          <div class="feedback-modal">
            <div class="modal-overlay">
              <div class="modal-content">
                <div class="modal-header">
                  <h3><i class="far fa-message"></i>&nbsp;&nbsp;FeedBack</h3>
                  <button class="modal-close" fdprocessedid="3zaroc">
                    <i class="fas fa-times"></i>
                  </button>
                </div>
                <div class="modal-body">
                  <form class="gb-form">
                    <div class="form-group coh">
                      <p class="block text-sm font-medium text-white mb-2">Overall Feedback</p>
                      <div class="flex justify-around items-center pt-2">
                        <label class="emoji-option text-center">
                          <input type="radio" name="rating" value="1" class="hidden" required="">
                          <span class="emoji-icon">😠</span>
                          <p class="text-white text-xs mt-2">Bad</p>
                        </label>
                        <label class="emoji-option text-center">
                          <input type="radio" name="rating" value="2" class="hidden">
                          <span class="emoji-icon">😞</span>
                          <p class="text-white text-xs mt-2">Poor</p>
                        </label>
                        <label class="emoji-option text-center">
                          <input type="radio" name="rating" value="3" class="hidden">
                          <span class="emoji-icon">😐</span>
                          <p class="text-white text-xs mt-2">Okay</p>
                        </label>
                        <label class="emoji-option text-center">
                          <input type="radio" name="rating" value="4" class="hidden">
                          <span class="emoji-icon">🙂</span>
                          <p class="text-white text-xs mt-2">Good</p>
                        </label>
                        <label class="emoji-option text-center">
                          <input type="radio" name="rating" value="5" class="hidden">
                          <span class="emoji-icon">😎</span>
                          <p class="text-white text-xs mt-2">Awesome</p>
                        </label>
                      </div>
                    </div>
                    <div class="form-row">
                      <div class="form-group">
                        <input type="text" placeholder="Full Name" required="" name='name' fdprocessedid="0hq8kj">
                        <i class="fas fa-user"></i>
                      </div>
                      <div class="form-group">
                        <input type="email" placeholder="Email Address" required="" name='email' fdprocessedid="2kv2wy">
                        <i class="fas fa-envelope"></i>
                      </div>
                    </div>
                    <div class="form-group">
                      <textarea placeholder="Additional notes or questions" rows="3" name='msg'></textarea>
                      <i class="fas fa-comment"></i>
                    </div>
                    <button type="submit" class="submit-btn" fdprocessedid="laipz9">
                      <i class="fas fa-phone"></i>
                      Submit
                    </button>
                  </form>
                </div>
              </div>
            </div>
          </div>
        <div>
    `;

  // Add modal styles
  const mms = `
        .feedback-modal {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 10002;
            animation: fadeIn 0.5s ease;
        }
        .coh p{color: rgb(0 0 0 / 0);background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);background-clip: text;}
        .modal-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.8);
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        .modal-content {
            background: white;
            border-radius: 20px;
            max-width: 600px;
            width: 100%;
            max-height: 90vh;
            overflow-y: auto;
            animation: slideInUp 0.5s ease;
        }
        .modal-header {
            padding: 30px 30px 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .modal-header h3 {
            font-size: 1.5rem;
            font-weight: 600;
            color: rgb(0 0 0 / 0);
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    background-clip: text;
        }
        .modal-close {
            background: none;
            border: none;
            font-size: 1.5rem;
            color: #999;
            cursor: pointer;
            transition: color 0.3s ease;
        }
        .modal-close:hover {
            color: #333;
        }
        .modal-body {
            padding: 30px;
        }
        .gb-form {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }
        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }
        @media (max-width: 480px) {
            .form-row {
                grid-template-columns: 1fr;
            }
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        @keyframes slideInUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    `;

  // Add styles to document
  const ss = document.createElement('style');
  ss.textContent = mms;
  document.head.appendChild(ss);

  document.body.appendChild(m2);
    _sl();
  // Close modal functionality
  const cb2 = m2.querySelector('.modal-close');
  const ol2 = m2.querySelector('.modal-overlay');

  cb2.addEventListener('click', cm);
  ol2.addEventListener('click', (e) => {
    if (e.target === ol2) cm();
  });

  function cm() {
    m2.style.animation = 'fadeOut 0.3s ease';
    setTimeout(() => {
      m2.remove();
      ss.remove();
    }, 300);
  }

  // Handle form submission
  const __cF = m2.querySelector('.gb-form');
  __cF.addEventListener('submit', function (e) {
    e.preventDefault();

    // Simulate appointment booking
    sn('🎉 Message sent! Thanks for reaching out. We’ll get back to you as soon as possible.', 'success');
    cm();
  });

  var eo = document.querySelectorAll('.emoji-option');
  eo.forEach(oj => {
    var i = oj.querySelector('input'), io = oj.querySelector('.emoji-icon');
    i.addEventListener('change', () => {
      document.querySelectorAll('.emoji-icon').forEach(j => j.style.filter = 'none');
      io.style.filter = 'drop-shadow(0 0 8px rgba(0, 0, 0, 0.8))';
    });
  });
}
